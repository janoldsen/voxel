#ifndef JOLD_RENDERER_H
#define JOLD_RENDERER_H

#include "application.h"


void init_renderer(struct renderer *renderer, window_handle window);

void update_renderer(struct renderer *renderer);



#endif
